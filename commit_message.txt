Ramin Yazdani | hierarchical-clustering-bio-data | main | WIP(expansion): Create notebook for periodic gene expression analysis

Expanded project scope to time-series analysis by creating a second notebook (periodic_expression.ipynb). Added initial cells for loading the periodic_gene_expression.tsv dataset and inspecting its structure. This notebook will analyze periodic expression patterns during the cell cycle.

The periodic expression dataset contains 8,812 genes measured across 57 time points, representing gene activity during cell cycle progression. This demonstrates how clustering and pattern detection techniques apply to time-series biological data, complementing the static methylation profile analysis.

Initial notebook includes data loading, dimension inspection, and basic data quality checks.

Verified: Gene expression data loads correctly, dimensions (8812, 57) confirmed, ready for visualization and peak detection implementation.
