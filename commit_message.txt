Ramin Yazdani | hierarchical-clustering-bio-data | main | WIP(init): Initial project setup with structure and dependencies

Created the foundational project structure for hierarchical clustering analysis of biological data. Established Python environment with requirements.txt containing core scientific computing dependencies (numpy, pandas, scikit-learn, matplotlib, scipy, jupyter). Added .gitignore to prevent committing Python cache files and notebook checkpoints. Created initial README.md documenting project purpose, scope, and planned folder structure.

This commit represents day 1 initialization work, setting up the project foundation before any implementation. All dependencies are specified for reproducibility. The README provides clear project description and establishes the bioinformatics analysis goals.

Verified: requirements.txt contains all necessary packages for clustering and visualization work ahead.
